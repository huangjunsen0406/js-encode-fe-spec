import{_ as s,o as t,c as n,a3 as a}from"./chunks/framework.BwtvuMfj.js";const g=JSON.parse('{"title":"@huangjunsen/stylelint-config","description":"","frontmatter":{},"headers":[],"relativePath":"packages/stylelint-config.md","filePath":"packages/stylelint-config.md"}'),l={name:"packages/stylelint-config.md"};function o(i,e,c,d,r,p){return t(),n("div",null,[...e[0]||(e[0]=[a(`<h1 id="huangjunsen-stylelint-config" tabindex="-1">@huangjunsen/stylelint-config <a class="header-anchor" href="#huangjunsen-stylelint-config" aria-label="Permalink to &quot;@huangjunsen/stylelint-config&quot;">​</a></h1><p>CSS、SCSS、Less 样式代码的共享质量检查规范。</p><hr><h2 id="包含规范" tabindex="-1">包含规范 <a class="header-anchor" href="#包含规范" aria-label="Permalink to &quot;包含规范&quot;">​</a></h2><ul><li>继承自标准的 <code>stylelint-config-standard</code> 与 <code>stylelint-config-standard-scss</code>；</li><li>禁止选择器重复与未知的属性；</li><li>避免属性简写覆盖详细属性；</li><li><strong>国内移动端兼容</strong>：放宽并支持小程序的 <code>rpx</code> 等自定义单位；</li><li>不含排版与空白类规则（缩进、换行、分号、行长度等统一交给 Prettier）。</li></ul><blockquote><p>要求 <code>stylelint</code> <code>^16.0.0 || ^17.0.0</code>。</p></blockquote><hr><h2 id="stylelint-16-起-排版规则已移除" tabindex="-1">stylelint 16 起：排版规则已移除 <a class="header-anchor" href="#stylelint-16-起-排版规则已移除" aria-label="Permalink to &quot;stylelint 16 起：排版规则已移除&quot;">​</a></h2><p>stylelint 16 把全部<strong>排版与空白类（stylistic）规则</strong>移交给了 Prettier，本配置已同步清理，不再声明：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>indentation                          max-line-length</span></span>
<span class="line"><span>no-extra-semicolons                  no-eol-whitespace</span></span>
<span class="line"><span>no-missing-end-of-source-newline     value-list-comma-space-after</span></span>
<span class="line"><span>declaration-block-trailing-semicolon block-opening-brace-newline-after</span></span>
<span class="line"><span>block-opening-brace-space-after      block-opening-brace-space-before</span></span>
<span class="line"><span>block-closing-brace-newline-before   block-closing-brace-space-before</span></span>
<span class="line"><span>color-hex-case                       string-quotes</span></span>
<span class="line"><span>unicode-bom</span></span></code></pre></div><p>在 stylelint 17 下继续声明这些规则会直接报 <code>Unknown rule ...</code>。若项目配置里还留着它们，需要一并删除。</p><hr><h2 id="使用方式" tabindex="-1">使用方式 <a class="header-anchor" href="#使用方式" aria-label="Permalink to &quot;使用方式&quot;">​</a></h2><p>在项目根目录创建 <code>stylelint.config.js</code>：</p><div class="language-javascript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">exports</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  extends: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;@huangjunsen/stylelint-config&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><h3 id="配置文件位置-新格式与旧格式" tabindex="-1">配置文件位置：新格式与旧格式 <a class="header-anchor" href="#配置文件位置-新格式与旧格式" aria-label="Permalink to &quot;配置文件位置：新格式与旧格式&quot;">​</a></h3><table><thead><tr><th>格式</th><th>文件名</th><th>状态</th></tr></thead><tbody><tr><td><strong>新（官方推荐）</strong></td><td><code>stylelint.config.{js,cjs,mjs,ts}</code></td><td>当前推荐位置</td></tr><tr><td>旧（仍支持）</td><td><code>.stylelintrc</code>、<code>.stylelintrc.{js,cjs,mjs,ts,json,yaml,yml}</code></td><td>官方明确标注「we may remove these in the future」</td></tr><tr><td>旧（仍支持）</td><td><code>package.json</code> 中的 <code>stylelint</code> 字段</td><td>同上</td></tr></tbody></table><blockquote><p>⚠️ <strong>注意优先级倒置</strong>：根据 stylelint 自带的 CLI 帮助文本，搜索顺序为 <code>package.json</code> 的 <code>stylelint</code> 字段 → <code>.stylelintrc</code> → <code>.stylelintrc.{cjs,mjs,js,ts,json,yaml,yml}</code> → <code>stylelint.config.{cjs,mjs,js,ts}</code>。</p><p>也就是说 <strong><code>.stylelintrc.*</code> 会遮蔽 <code>stylelint.config.*</code></strong>。两者同时存在时只有前者生效， 建议项目内只保留一种。</p></blockquote><h3 id="与-encode-fe-lint-的协作方式" tabindex="-1">与 <code>encode-fe-lint</code> 的协作方式 <a class="header-anchor" href="#与-encode-fe-lint-的协作方式" aria-label="Permalink to &quot;与 \`encode-fe-lint\` 的协作方式&quot;">​</a></h3><p><code>encode-fe-lint</code> 按以下优先级确定 stylelint 配置：</p><ol><li>项目中存在 <code>.stylelintrc.*</code>、<code>stylelint.config.{js,cjs,mjs,ts}</code>，或 <code>package.json</code> 中有 <code>stylelint</code> 字段 → 交由 stylelint 自行发现并加载，<strong>不会</strong>被内置预设覆盖；</li><li>以上均无 → 使用 <code>@huangjunsen/stylelint-config</code> 作为默认配置。</li></ol><p>当项目有自有配置时，<code>encode-fe-lint</code> 会优先使用<strong>项目内安装的 stylelint</strong> 执行。 因为 stylelint 16 起，配置里裸模块名形式的 <code>customSyntax</code>（Vue 项目常用的 <code>postcss-html</code>） 是从 stylelint 自身所在位置解析的，而不是从项目目录解析 —— 用本包内置的 stylelint 会找不到项目里安装的这些模块，从而报 <code>Cannot resolve custom syntax module &quot;postcss-html&quot;</code>。</p><blockquote><p>自 <code>encode-fe-lint@1.0.18</code> 起支持该行为。 此前版本请确保项目内也安装了 stylelint，或把 <code>customSyntax</code> 改为 <code>require.resolve(...)</code> 的绝对路径。</p><p>自 <code>encode-fe-lint@1.0.15</code> 起支持 <code>stylelint.config.js</code>。 在此之前，项目自带该文件时会被内置预设静默覆盖，导致项目配置失效。</p></blockquote>`,23)])])}const y=s(l,[["render",o]]);export{g as __pageData,y as default};
